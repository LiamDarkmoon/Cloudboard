---
import AuthForm from '../components/AuthForm.astro';
import Layout from '../layouts/Layout.astro';

if (Astro.request.method === "POST") {
  if (Astro.response.status === 200) return Astro.redirect("/dashboard");
}
---

<Layout>
	<AuthForm/>
</Layout>